using UnityEngine;

public class Triggers_Juego1 : MonoBehaviour
{
    /*
    private bool Correcto;
    public GameObject NotaCorrecta;
    public GameObject[] NotasIncorrectas;

    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject==NotaCorrecta)
        {
            Correcto = true;
            Debug.Log("Correcto");
        }
        else
        {
            foreach(GameObject nota in NotasIncorrectas)
            {
                if (other.gameObject == nota)
                {
                    Correcto = false;
                    Debug.Log("Incorrecto");
                    break;
                }
            }
        }
    }*/
}
